Complete C4 matrix assembly and first solve

Finish matrixAssembly: vortex column A[i][N] as a sum over j, b[i]
from the freestream, and the Kutta row with its two self-influence
cases at j = 0 and j = N-1.

Fix sign error in angleCalculation. Returns theta_2 - theta_1 so that
source self-influence is +1/2 along the outward normal, consistent with
the hardcoded diagonal. Previously the off-diagonal normal component
carried the opposite sign to the diagonal, which left the alpha = 0
symmetry test intact but broke mass closure.

Add Vec2 operators (dot, +, * by scalar) inline in vec2.h.

Validation:
  gamma = -5.7e-18 at alpha = 0 for 0012
  closure sum 1.98e-3, 9.7e-4, 4.8e-4 at 50/100/200 points per surface
  lift slope 6.895, 6.577, 6.366 per radian for t = 0.12, 0.06, 0.02
  excess over 2pi scales linearly with thickness
